{
  "name": "atom-repos",
  "private": true,
  "scripts": {
    "setup": "lerna bootstrap",
    "release": "lerna publish --force-publish",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.0"
  }
}
